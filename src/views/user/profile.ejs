<!DOCTYPE html>
<html>
<head>
    <title>My Profile - WriteAce</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/css/stylesheet.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-3xl mx-auto">
            <div class="bg-white rounded-lg shadow p-6">
                <!-- Header Section with Avatar -->
                <div class="flex items-center space-x-4 mb-8 border-b pb-6">
                    <div class="relative">
                        <img src="<%= user.avatar %>" 
                             alt="Profile Picture" 
                             class="w-16 h-16 rounded-full object-cover">
                        <button id="changeAvatarBtn" 
                                class="absolute bottom-0 right-0 bg-blue-500 text-white p-1 rounded-full hover:bg-blue-600 w-6 h-6 flex items-center justify-center hidden">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                                      d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                            </svg>
                        </button>
                    </div>
                    <div>
                        <h2 class="text-xl font-semibold"><%= user.username %></h2>
                        <p class="text-gray-500"><%= user.email %></p>
                    </div>
                    <button type="button" 
                            id="editProfileBtn"
                            class="ml-auto bg-blue-500 text-blue-500 px-4 py-2 rounded-md hover:bg-blue-600 text-sm">
                        Edit
                    </button>
                </div>

                <!-- Profile Form -->
                <form id="profileForm" action="/profile/update" method="POST" class="space-y-6">
                    <div class="grid grid-cols-2 gap-4">
                        <!-- Full Name -->
                        <div class="col-span-2 sm:col-span-1">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Full Name</label>
                            <input type="text" name="username" value="<%= user.username %>" 
                                   class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500"
                                   disabled>
                        </div>

                        <!-- Phone -->
                        <div class="col-span-2 sm:col-span-1">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
                            <input type="tel" name="phone" value="<%= user.phone || '' %>" 
                                   class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500"
                                   disabled>
                        </div>

                        <!-- Email -->
                        <div class="col-span-2 sm:col-span-1">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                            <input type="email" name="email" value="<%= user.email %>" 
                                   class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500"
                                   disabled>
                        </div>

                        <!-- Gender -->
                        <div class="col-span-2 sm:col-span-1">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Gender</label>
                            <select name="gender" 
                                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500"
                                    disabled>
                                <option value="male" <%= user.gender === 'male' ? 'selected' : '' %>>Male</option>
                                <option value="female" <%= user.gender === 'female' ? 'selected' : '' %>>Female</option>
                                <option value="other" <%= user.gender === 'other' ? 'selected' : '' %>>Other</option>
                            </select>
                        </div>

                        <!-- Address -->
                        <div class="col-span-2">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Address</label>
                            <textarea name="address" rows="2" 
                                      class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500"
                                      disabled><%= user.address || '' %></textarea>
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <div class="flex justify-end pt-4 hidden" id="saveButtons">
                        <button type="button" 
                                id="cancelBtn"
                                class="bg-gray-100 text-gray-700 px-4 py-2 rounded-md hover:bg-gray-200 text-sm mr-2">
                            Cancel
                        </button>
                        <button type="submit" 
                                class="bg-blue-500 text-blue-600 px-4 py-2 rounded-md hover:bg-blue-600 text-sm">
                            Save Changes
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <input type="file" id="avatarInput" accept="image/*" style="display: none;">
    <script src="/js/profile.js"></script>
</body>
</html>